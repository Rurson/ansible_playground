hosts:
  localhost:
    vars:
      ansible_host: 127.0.0.1
      anisble_connection: local
      anisble_user: root
      ansible_sudo: yes
      ansible_python_interpreter: "{{ansible_playbook_python}}"
  docker:
    vars:
      anible_host: anisbleplayground_ubuntu_1
      anisble_user: root

